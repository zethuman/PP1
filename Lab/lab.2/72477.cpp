#include <iostream>

using namespace std;

int main ()
{
    int n , a[i];
    cin >> n;
    
    for ( i=0; i < n ; i = i+1 )

    cin >> a[i];
    

    for ( i=0; i < n ; i = i+1 )
    {
        if (a[i] % 2 == 1 )
        cout << a[i] << endl;
    }
}