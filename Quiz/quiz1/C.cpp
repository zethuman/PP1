#include <iostream>
#include <math.h>
using namespace std;

int main ()
{
    int  a, b, n ;
    cin >> a >> b;
    int dif = 1000000000;

    for (a = 1; a <= n; a++)
    {
        if (n % a == 0)
    {
        b = n / a;}
        if (a <= b)
        {
        if  (abs(b - a < dif))
    
    }
    }
      cout << a << b;
    }