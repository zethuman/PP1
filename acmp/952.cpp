#include<iostream>
#include<cmath>

using namespace std;

int main()
{
 int a,b;
 cin >> a >> b;


 if(a == 0)
 {
   if(b == 0)
   {
     cout << "Impossible";
   }
   else cout << "Impossible";
 }
 else if(a != 0 && b == 0)
 {
   cout << a << " " << a ;
 }
  else
 cout << b << " " << a + b - 1;

} 